import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    width: 255,
    height: 260,
    margin: 20,
    background: "#ececec",
  },
});

export default useStyles;
