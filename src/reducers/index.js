const initState = {
  fCards: [],
  totalIncome: 0,
  totalExpense: 0,
};

export const rootReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
