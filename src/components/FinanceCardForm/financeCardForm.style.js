import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  title: {
    fontSize: 14,
    fontWeight: "bold",
    textAlign: "center",
  },
});

export default useStyles;
